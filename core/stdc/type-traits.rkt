(define (is-void t) 0)
(define (is-null-pointer t) 0)
(define (is-integral t) 0)
(define (is-floating-point t) 0)
(define (is-array t) 0)
(define (is-enum t) 0)
(define (is-union t) 0)
(define (is-class t) 0)
(define (is-function t) 0)
(define (is-pointer t) 0)  
(define (is-lvalue-reference t) 0)
(define (is-rvalue-reference t) 0)
(define (is-member-object-pointer t) 0)
(define (is-member-function-pointer t) 0) 
(define (is-fundamental t) 0)
(define (is-arithmetic t) 0)  
(define (is-scalar t) 0)
(define (is-object t) 0)
(define (is-compound t) 0)
(define (is-reference t) 0)
(define (is-member-pointer t) 0)
(define (is-const t) 0)
(define (is-volatile t) 0)
(define (is-trivial t) 0)
(define (is-trivially-copyable t) 0)
(define (is-standard-layout t) 0)
(define (is-pod t) 0)
(define (is-literal-type t) 0)
(define (has-unique-object-representations t) 0)
(define (is-empty t) 0)
(define (is-polymorphic t) 0)
(define (is-abstract t) 0)
(define (is-final t) 0)
(define (is-aggregate t) 0)
(define (is-signed t) 0)
(define (is-unsigned t) 0)
(define (is-bounded-array t) 0)
(define (is-unbounded-array t) 0)
(define (is-constructible t) 0)
(define (is-trivially-constructible) 0)
(define (is-nothrow-constructible t) 0)
(define (is-default-constructible t) 0)
(define (is-trivially-default-constructible t) 0)
(define (is-nothrow-default-constructible t) 0)
(define (is-copy-constructible t) 0)
(define (is-trivially-copy_constructible t) 0)
(define (is-nothrow-copy-constructible t) 0)
(define (is-move-constructible t) 0)
(define (is-trivially-move-constructible t) 0)
(define (is-nothrow-move-constructible t) 0)
(define (is-assignable t) 0)
(define (is-trivially-assignable t) 0)
(define (is-nothrow-assignable t) 0)
(define (is-copy-assignable t) 0)
(define (is-trivially-copy-assignable t) 0)
(define (is-nothrow-copy-assignable t) 0)
(define (is-move-assignable t) 0)
(define (is-trivially-move-assignable t) 0)
(define (is-nothrow-move-assignable t) 0)
(define (is-destructible t) 0)
(define (is-trivially-destructible t) 0)
(define (is-nothrow-destructible t) 0)
(define (has-virtual-destructor t) 0)
(define (is-swappable-with t) 0)
(define (is-swappable t) 0)
(define (is-nothrow-swappable-with t) 0)
(define (is-nothrow-swappable t) 0)
(define (is-same t) 0)
(define (is-base-of t) 0)
(define (is-convertible t) 0)
(define (is-nothrow-convertible t) 0)
(define (is-invocable t) 0)
(define (is-invocable_r t) 0)
(define (is-nothrow-invocable t) 0)
(define (is-nothrow-invocable-r t) 0)
(define (is-constant-evaluated t) 0)
  
